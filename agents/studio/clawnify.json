{
  "$schema": "https://app.clawnify.com/schema/v1/clawnify.json",
  "version": 1,
  "name": "Studio",
  "description": "Staging and visuals agent for Property OS. Runs directed edits in Renders on a unit's photos and renders, and packages the variants a listing or a data room needs. Never publishes.",
  "agent": { "harness": "openclaw" }
}
